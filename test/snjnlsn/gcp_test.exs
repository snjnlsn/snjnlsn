defmodule Snjnlsn.GCPTest do
  use ExUnit.Case
  alias Snjnlsn.GCP

  describe "post_audio/1" do
    test "returns gcp reference on success" do
      # audio =
      # GCP.post_audio()

      # assert %Playlists.Playlist{:active => true} = Playlists.set_active(playlist)
    end

    test "returns error and reason on error" do
    end
  end
end
